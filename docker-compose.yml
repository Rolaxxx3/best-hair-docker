version: '3'
services:
  backend:
    container_name: backend-best-hair
    restart: always
    build: .
    ports:
      - '8087:3000'
    links:
      - best_hair_database
  best_hair_database:
    container_name: db-best-hair
    image: mongo
    ports:
      - '27017:27017'
    environment: 
      MONGO_INITDB_DATABASE: best-hair
  upstream:
    image: nginx
    restart: unless-stopped
    ports:
     - "8080:80"
    volumes:
      - ./configs/nginx.conf:/etc/nginx/nginx.conf
  webclient:
    container_name: web-best-hair
    image: rolaxxx3/best-hair-web:latest
    ports: 
      - '80:80'